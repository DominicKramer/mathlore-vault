ProtoResult:
. "If $f'$ exists and does not change sign on $(a,b)$ then $f$
   is monotonic on $(a,b):$ increasing, nondecreasing, 
   decreasing, or nonincreasing as $$ f'(x)>0, f'(x)\ge0, 
   f'(x)<0, f'(x) \le0, $$ respectively, for all $x$ in 
   $(a,b)$"
Metadata:
. reference:
  . source: "@TrenchRealAnalysis"
    page: "86"
    offset: "95"
. id: "5e23b2357be8017205ed44e6"

